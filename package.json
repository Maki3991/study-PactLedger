{
  "name": "pactledger",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix web",
    "build": "npm run build --prefix web",
    "lint": "npm run lint --prefix web",
    "test:api": "npm run test:api --prefix web",
    "start:api": "npm run start:api --prefix web"
  }
}
