# Agent Treasury 双租户完整方案规划 v3
## 租户一:KaleidoX(AI 交易团队)|租户二:PoolMate(群聊拼单结算 Agent)

> ⚠️ 命名纪律:所有参赛物料(pitch、README、提交表、演示)中不出现公司名。项目对外统一为 **KaleidoX**(场景层)+ **Agent Treasury**(基建层)。

> **一套 Agent 财务基建(Treasury),两个真实租户:一支会自己花钱、被合约管着、靠市场机制进化的 AI 交易团队(KaleidoX),和一个住在群聊里、替全群管钱的拼单结算 agent(PoolMate)。**

---

## 0. 一句话定位

**我们做的是 AI agent 的财务系统(Agent Treasury)。租户一选交易,因为它是最严苛的压力测试——AI 花错钱会立刻亏损;租户二选群聊拼单,因为它是最贴近普通人的场景——AI 替一群人管钱。一套基建同时撑起两个毫不相干的场景,才叫基础设施。**

对外三个层次的说法:

| 受众 | 说法 |
|---|---|
| 赛道 18 评委 | 一支由市场机制驱动协作与进化的 AI 投研交易团队(KaleidoX) |
| 赛道 01 评委 | Injective 上的 agent 经济结算与审计基础设施 + 两个真实租户应用 |
| 赛道 24 评委 | 一个消息原生、有真实钱包、权限被合约锁死的群聊拼单 agent(PoolMate) |

---

## 1. 赛道策略

| 赛道 | 权重 | 打法 |
|---|---|---|
| 01 Injective(奖池 10 万) | 主攻 | gateway 从 HashKey 迁移至 Injective + 实质性新增(Treasury 模块、多协议归一层适配、agent 间结算) |
| 18 PandaAI(奖池 3 万) | 主攻 | DeepSeek V4 底座、A2A Remote Agent 提交、市场机制多 agent 协作作为核心差异化 |
| 04 Qoder | 沾边 | 全程用 Qoder 开发,留协作记录,提交时讲"4 人 + AI 专家团 = 一支完整工程队" |
| 24 Photon | 次主攻 | **PoolMate 作为独立参赛项目**(赛道文档点名的「群聊拼单/记账」方向)+ 兼作 KaleidoX 的委托入口与播报出口;评委手机入群即可体验 |

---

## 2. 总体架构:物理分层的两块

```
┌─────────────────────────────────────────────┐
│  租户一:KaleidoX(交易场景)                 │
│  Research/Strategy/Backtest/Risk/            │
│  Evolution/Execution 六个 Agent              │
│  ─────────────────────────────              │
│  租户二:PoolMate(群聊拼单/代购结算)        │
│  消息交互层 + 演示商户端 + 拼单状态机        │
├─────────────────────────────────────────────┤
│  下层基建:Agent Treasury                    │
│  · Agent 钱包开设与预算分配                  │
│  · 消费策略引擎(单笔上限/白名单/日限额)     │
│  · Protocol Router(x402 / ACP / AP2 归一化)│
│  · 计费结算 + 审计账本                       │
│  ※ 不含任何交易逻辑,可单独部署给任意场景    │
├─────────────────────────────────────────────┤
│  结算层:Injective(EVM,测试网执行/主网部署)│
│  基于既有开源多协议结算 gateway 迁移          │
└─────────────────────────────────────────────┘
```

分层纪律:代码仓库按此切目录,架构图按此画,下层(Treasury)目录中禁止出现任何交易或拼单语义的代码。两个租户共用 Treasury 且 Treasury 零改动——这是「基础设施」宣称成立的前提,也是全场最硬的一个证明。

---

## 3. 三种资金流 × 协议分层(Protocol Router 的必经之路)

系统中所有的钱只有三种流法,每种对应一个协议,全部汇入 Router 归一化后落 Injective 结算:

**流 1|人 → AI 团队(委托关系)→ ACP / AP2**
用户下达委托、支付服务费、结算分润。commerce 类协议原生支持授权范围、争议与退款语义,正好承载"人授权 agent 消费"。

**流 2|AI ↔ AI(高频微交易)→ x402**
agent 之间购买研究报告、回测服务、风控审核,单笔小额、按次结清。x402 的 per-request 设计就是为这种场景生的,Injective 亚秒出块 + 近零 gas 让它真正可行。

**流 3|AI → 外部服务(数据采购)→ x402**
Research Agent 需要外部付费数据源时,自主完成 402 握手、付款、取数,收据哈希写入研究报告(provenance)。

三条流统一经过:**Protocol Router 归一化 → canonical checkout → Compliance Gate 检查 → Injective 结算 → 审计账本记录**。

**关键设计:两个租户的资金流完全同构。** KaleidoX 的「用户委托→agent 间采购→分润」与 PoolMate 的「群友委托→向商户 checkout→分账退差」是同一组原语(委托 / 消费 / 分配)在两个场景的实例化——这正是 Router 与 Treasury 场景无关的证明:

| 原语 | 租户一 KaleidoX | 租户二 PoolMate | 协议 |
|---|---|---|---|
| 委托(人→agent) | 用户下达交易委托 | 群友确认拼单份额 | ACP / AP2 |
| 消费(agent→服务方) | agent 间买研究/回测/风控 + 外部数据采购 | 向商户完成组单付款 | x402 / AP2 |
| 分配(agent→人) | performance fee 分润 | 退差价、摊运费(批量 payout) | 批量 payout |

Router 由此从"展示柜功能"变成每一笔钱的必经之路——这是该多协议归一层(Protocol Router)的第一次全场景实战。

---

## 4. Agent 团队设计:同事变交易对手

六个 Agent 角色沿用 KaleidoX 原案,但关系重构:**每个 agent 持有独立链上钱包,一切协作皆为交易。**

| Agent | 职责 | 经济关系 |
|---|---|---|
| Planner/Orchestrator | 接收用户委托,持有总预算 | 不分配任务,而是**采购服务**;预算向历史表现好的 agent 倾斜 |
| Research | 市场研究(PandaAI 数据 skills + 外部付费源) | 出售研究报告(按份计价);采购外部数据(x402) |
| Strategy | 生成候选策略 | 花钱购买研究报告;出售策略方案 |
| Backtest | 回测验证(PandaAI 回测/绩效 skills) | 验证服务按次收费;不合格退回但费用照收 |
| Risk | 独立风控,拥有否决权 | 审核收费,**否决不退款**——让"叫风控"有真实成本,倒逼 agent 先自查 |
| Execution | 生成 Injective 交易指令并执行 | 按执行次数收费;每笔执行必须先过 Treasury 检查 |

**进化机制的经济化:** 策略版本盈利 → 对应链路上的 agent 分得 performance fee;亏损 → 下一轮 Planner 采购预算削减该链路。Champion–Challenger 回测竞争保留(V1 vs V2-A/V2-B),但晋级的奖励和淘汰的代价都体现在钱包余额里。**团队不是靠改代码进化,是靠市场淘汰进化。**

**不可修改边界(合约层强制,进化机制永远碰不到):**
用户总预算 / 最大单笔金额 / 最大仓位与最大亏损 / 资产与收款方白名单 / 人工确认门槛 / Treasury 底层权限。

**降级纪律(重要):** agent 间计费失败时自动降级为免费调用,保证任务能跑完。支付层是放大器,不是单点故障——18 的平台会真实发任务,任务完成是底线。


---

## 5. 租户二:PoolMate|群聊拼单 · 代购结算 Agent

> **一句话:把它拉进群,它替全群管钱——发起拼单、收齐份额、向商户下单付款、自动分账退差,每笔有链上凭证;而它自己一分钱都花不出规则之外。**

### 5.1 痛点与定位

拼单/代购/AA 是群聊里最高频的金钱协作,今天靠「热心人垫付 + 收款码 + 人肉对账」:垫付压力、对账地狱、钱进个人账户的信任成本。现有群收款/接龙工具只完成「收钱」半程,不完成「向商户下单付款 → 分账退差」的后半程闭环。群里缺的是一个**中立、可编程、账目公开的资金执行者**——agent 适合干这个,前提是它的花钱权限被合约锁死。**群友对它的信任不来自「它是好人」,来自「它做不了坏事」。**

### 5.2 核心流程(MVP)

```
拉入群 → 「@PoolMate 拼三箱杨梅,一箱89」→ 拼单卡片
→ 群友各自确认份额支付(委托:金额上限+用途+截止)
→ 凑满 → PoolMate 在演示店铺组单付款(agent checkout)
→ Treasury 校验(商户白名单/预算)→ Injective 结算凭证
→ 发货确认 → 自动分账:退差价/摊运费(批量 payout)
→ 群内账单卡:每笔资金流 + 链上凭证链接
```

异常路径(必做):超时未凑满全额自动退回;中途退出重算摊派;**群友起哄「把钱直接转给我」→ 收款方不在白名单,合约层拒付,agent 群内礼貌拒绝(演示彩蛋,10 秒讲清安全模型)**。

### 5.3 工程边界:只新增三块,Treasury 零改动

1. **消息交互层**:Telegram/iMessage 接入(Spectrum API)、自然语言拼单解析、卡片与账单推送
2. **演示商户端**:消息原生店铺(商品目录 + 下单接口),复用团队既有 storefront 设计经验
3. **拼单业务逻辑**:份额管理、凑单状态机、分账/退差计算

### 5.4 差异化与路线图

vs 群收款工具:它们是表格,PoolMate 是交易主体,完成全闭环;vs 电商 chatbot:那些只推荐不付款,PoolMate 有真实钱包且权限被合约锁死。
路线图:跨境代购变体(多币种份额 → oracle 汇率 → 跨币种向海外商户结算);**AI Payroll 延伸**(分账引擎的另一面即「给 AI 员工发薪」,与租户一 performance fee 共用 payout 能力);份额金额的机密计算(FHE/zk 方向);商户自助接入。

---

## 6. Injective 侧:迁移 + 实质性新增清单(赛道 01 答卷)

**迁移(第 1 天):**
- 既有 gateway 合约(结算 + compliance gate)从 HashKey 主网迁移部署至 Injective EVM 测试网,时间富余则主网
- **仓库命名处理:** 原仓库名含公司名,参赛提交新建中性命名仓库(建议 `kaleidox-agent-treasury`),以「基于本团队既有开源结算层项目迭代」方式在 README 中注明技术来源,同时满足赛道「实质性新增功能」规则与命名纪律
- 记录迁移中的适配差异 → 直接回答 01 提交问题"开发过程遇到什么困难 / 文档哪里帮到你 / 哪里需改进"

**实质性新增(比赛期间,满足赛道规则):**
1. **Agent Treasury 模块**:子账户钱包、预算分配、消费策略(单笔/日限/白名单)、超限合约级拒付
2. **多协议归一层的 Injective 适配**:x402 / ACP / AP2 三种入口统一落 Injective 结算
3. **Agent 间计费结算**:高频微支付通道 + 审计账本事件
4. **Provenance 收据**:每笔采购生成结构化链上事件,供报告引用

**"为什么必须是 Injective"的答案:** 一个任务周期产生几十笔 agent 间微支付,只有亚秒出块 + 近零 gas 的链能让"每次协作都是交易"在经济上成立。这不是把 AI 报告上链盖章,而是链承担了结算 + 权限约束 + 审计三种不可替代功能。

---

## 7. PandaAI 侧:合规接入清单(赛道 18 答卷)

**硬性要求逐条落实:**
- 底座模型:全部 agent 推理统一 DeepSeek V4 Pro(官方 token)
- 提交形式:A2A Remote Agent,自托管服务 + 公开 Agent Card URL(`/.well-known/agent-card.json`)
- 稳定性:评审期间保持在线;单任务总响应 ≤ 20 分钟(设 15 分钟软超时,超时输出已完成部分 + 说明)
- 通用任务路径:平台发来的任意自然语言金融任务,都能走完「研究 → 建议 → 风险提示」基本流;内部经济与进化剧情是加分展示,不是唯一路径
- 输出规范:结构化过程日志(每步含 agent 名、调用的 skill、产生的费用与 tx hash)+ 最终结论 + 强制风险提示段
- 使用的数据/投研 Skills 列表明确写入说明文档

**资产错配问题(开赛第一件事去展位确认):**
- **Plan A(默认):** 研究标的使用 PandaAI 覆盖的股票资产,充分调用其行情/财务/回测 skills;Injective 侧定位为「agent 资金权限、结算与执行记录层」——大脑研究证券,手管钱包,分工依然成立
- **Plan B(若 A 不可行或想保加密叙事):** 标的用 ETH,行情自接公开源,PandaAI 只用回测/绩效/报告类 skills,并在文档中如实说明
- 两个 Plan 的分支点只在 Research/Backtest 的数据适配层,架构不变

**差异化答辩点(别的队伍写不出的两段):**
1. 多 agent 协作 = 市场机制而非消息传递:采购、竞价、否决成本、预算倾斜——直接回应赛道原文「Agent 之间如何协作、通信、分工、反馈与持续优化」与「我们从未见过、却相信未来可能发生的产品」
2. 不同信任层级的 agent 交互需要不同支付协议,我们已完成归一化(Protocol Router)

---

## 8. Demo 脚本

> 主题:一笔失败交易,如何让一支「自己管钱」的 AI 团队进化出下一代策略。
> 预先准备:V1 策略交易提前在 Injective 测试网执行完毕;Photon 消息通道提前连好。

1. **委托下达(Photon 入口):** 评委/主持人在 iMessage 或 Telegram 里发出:「用 1,000 USDT 研究交易机会,最大亏损 5%,单一资产仓位 ≤ 30%」→ 大屏显示 Treasury 锁定三条不可修改规则 + ACP/AP2 委托结算凭证
2. **读取 V1 链上结果:** 展示 V1 的 Injective 测试网交易哈希与亏损归因入口;旁边并排展示当时每一步的 Treasury 检查记录
3. **失败归因:** Evolution Agent 输出结构化归因(信号过度依赖短期趋势 / 未识别震荡市 / 交易频率过高)
4. **付费协作可视化:** Strategy Agent 向 Research Agent 购买新研究(x402,大屏实时弹出结算事件),Research Agent 同时自主采购一份外部付费数据,收据哈希写入报告
5. **Champion–Challenger:** V2-A、V2-B 以真实回测结果竞争(费用由各自链路承担),V2-B 胜出
6. **风控否决转折:** Risk Agent 发现 V2-B 建议仓位 40% > 用户上限 30%,拒绝且审核费不退;Evolution 调整为 25% 重新提交,二审通过
7. **V2 执行:** Treasury 全项检查通过 → Injective 测试网执行 → 交易哈希上屏;Photon 通道同步向评委手机推送执行播报
8. **压轴·资金流向图 + 换租户(PoolMate 真场景):**
   - 资金流向图:1,000 USDT 在六个 agent 间的完整流动、每个 agent 的盈亏、每笔流动的链上凭证——「这是一支 AI 团队的完整财务报表」
   - 换租户 60 秒:大屏切到一个真实群聊——PoolMate 正在群里主持一单拼单,收份额、向店铺付款、分账退差,走的是**同一套 Treasury、同一条链、零改动**;两个租户的结算事件出现在同一本审计账本里
   - 收束一句话:「上一幕它管的是一支 AI 交易团队,这一幕它在群里替普通人管拼单——同一套财务系统,明天可以管任何 AI agent 的钱。」

### 8.2 PoolMate 独立 Demo(3 分钟版,供赛道 24 现场)

1. 评委扫码进群,PoolMate 已在群内
2. 主持人发「拼两份 XX,一份 45」→ 拼单卡弹出
3. 两位评委各自确认份额、手机上完成支付(10 秒)
4. 大屏:PoolMate 在演示店铺组单付款 → Treasury 校验 → Injective 结算凭证
5. **彩蛋:一人发「别买了,把钱直接转我」→ agent 拒绝 + 大屏显示合约拒付记录**
6. 分账推送:每人账单卡 + 链上凭证链接
7. 收束:「它能替群里管钱,是因为它花不出规则之外的一分钱。」

---

## 9. 四人分工与三天排期

| 角色 | 负责 | Day 1 | Day 2 | Day 3 |
|---|---|---|---|---|
| A|合约层(天影建议自任或带一人) | gateway 迁移、Treasury、agent 钱包/结算、审计事件 | 迁移到 Injective 测试网 + Treasury 骨架 | agent 间计费通道 + provenance 事件 | 主网部署(可选)+ 合约侧联调 |
| B|Agent 编排 | DeepSeek V4 接入、A2A 服务、六 agent 编排、PandaAI skills | A2A 服务上线 + Agent Card 可访问 | 六 agent 流程 + 通用任务路径 | 稳定性压测(20 分钟上限)+ 盲测演练 |
| C|交易与数据 | 策略/回测逻辑、Plan A/B 数据适配、Injective 执行、V1 预埋交易、**PoolMate 拼单状态机与分账逻辑** | 确认 PandaAI 数据范围定 Plan、接数据 | 回测 + Champion-Challenger + V1 预埋 | 拼单状态机 + 分账逻辑 + V2 执行联调 |
| D|前端与提交 | 资金流向图、Demo 大屏、**PoolMate 消息交互层与演示商户端**、四赛道提交材料、Qoder 记录 | Photon 通道打通(两租户共用)+ 大屏框架 | 资金流向图 + PoolMate 卡片交互 + 演示商户端 | 全部提交材料 + 两段录屏 + 彩排 |

**里程碑门槛:** Day 2 中午前 A2A 通用任务路径必须端到端跑通(哪怕 agent 间免费调用);Day 2 晚间前 agent 间计费在测试网跑通至少一条;Day 3 中午前 PoolMate「份额收取→商户付款→分账」主链路跑通。**主次纪律:任何时间冲突下,租户一(01/18 主赛道)优先于租户二。**任何一个没达标,启用第 10 节降级预案。

---

## 10. 风险与降级预案

| 风险 | 概率 | 预案 |
|---|---|---|
| PandaAI 数据只覆盖证券、Plan B 也受限 | 中 | 开赛 2 小时内展位确认;最坏情况回测用历史数据离线预算,现场展示缓存结果并如实标注 |
| A2A 盲测任务超时/跑崩 | 中 | 15 分钟软超时输出部分结果;支付层故障自动降级免费调用;编排层加断点续跑 |
| agent 间计费做不完 | 中 | 保底只做两条真实计费链路(Strategy→Research、Risk 审核费),其余展示为账本记录;叙事不变 |
| Injective 主网部署来不及 | 高 | 测试网即满足赛道规则,主网列为「已在 HashKey 主网验证、Injective 主网部署中」 |
| PoolMate 主链路做不完 | 中 | 三级降级:①砍代购/跨境只留基础拼单;②分账改为账本展示不真实出款;③整体降级为预录 60 秒视频,24 赛道仍可交(消息收发真实即满足其评审底线) |
| 拼单卡片/消息交互在 iMessage 与 Telegram 表现不一致 | 中 | 以 Telegram 为主演示渠道(卡片能力强),iMessage 保底纯文本流程,提交材料注明双渠道 |
| 交易表现不可控被质疑 | 低 | 叙事早已声明:测试资产、压力测试场景、评的是财务系统不是收益率 |

---

## 11. 四赛道提交问题映射(速查)

- **01 Injective:** 集成方式=gateway 迁移+四项新增(第 6 节);困难与文档反馈=迁移记录;Demo=第 8 节;仓库=原 repo 新增 injective 分支/目录
- **18 PandaAI:** Agent Card URL、skills 清单、示例任务、风险提示=第 7 节;架构图=第 2 节;差异化=市场机制协作
- **04 Qoder:** 人机分工叙述=「4 人定目标做决策,Qoder 多智能体承担前端/合约/测试专家角色」+ 全程录屏截图
- **24 Photon:** 项目主体=PoolMate(赛道点名的群聊拼单方向);一句话回答「为什么活在消息里」=拼单的发生地就是群聊,零安装是它成立的前提;消息原生性=群聊上下文理解+主动催收/播报+卡片+多渠道;演示=评委手机入群现场体验(第 8.2 节)+ 30 秒收发消息视频;渠道=Telegram 主 + iMessage;兼作 KaleidoX 委托入口(第 8 节第 1/7 幕)

---

## 12. 最终 Pitch(60 秒版)

> 今天的多 agent 系统,协作靠消息传递,进化靠人改代码。
> KaleidoX 把一支 AI 投研团队变成一个微型经济体:每个 agent 有自己的钱包,买研究、买回测、付风控审核费,每一笔都通过 Protocol Router 归一化(x402 / ACP / AP2 统一落链),在 Injective 上真实结算。干得好的 agent 赚到分成、接到更多单;干得差的被市场淘汰。策略从 V1 的失败中进化到 V2,全程被合约级的 Treasury 锁死在用户设定的风险边界内——AI 可以进化策略,永远不能进化自己的权限。
> 我们选交易做首个场景,因为它是最严苛的压力测试。而为了证明这是基础设施不是单一应用,我们把同一套 Treasury 接进了群聊:PoolMate,一个替全群管拼单的 agent——收份额、向商户付款、分账退差,权限同样被合约锁死,连群里骗它转账都会被合约层拒付。
> 一套财务系统,上午管一支 AI 交易团队,下午在群里替普通人管钱。
> **这不是一个 AI 交易机器人,这是 AI agent 时代的财务系统——它今天已经在两条链、两个场景里跑起来了。**
